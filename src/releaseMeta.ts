/**
 * Display version shown in-app (Settings → About) and **sidebar tab icon** cache-bust query (`widgetTabIcon` in `src/widgets/index.tsx`).
 * Keep in sync with `public/manifest.json` and `package.json` — see `docs/VERSIONING.md`.
 */
export const POKEREM_VERSION = '0.2.0';
