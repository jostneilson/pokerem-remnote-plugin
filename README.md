# Ankimon (RemNote)

RemNote plugin port of Ankimon: Pokémon-themed progression tied to flashcard reviews.

## Development

```bash
npm install
npm run dev
```

Load the dev server URL in RemNote’s plugin developer settings (see RemNote plugin docs).

```bash
npm run build
```

Produces `dist/` and `PluginZip.zip` for installation.
